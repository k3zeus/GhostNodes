#!/bin/bash
# Compatibility entrypoint: all installations use the same dashboard.
set -euo pipefail
GN_ROOT="${GN_ROOT:-$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)}"
export GN_ROOT
exec bash "${GN_ROOT}/ghostnode" "$@"
