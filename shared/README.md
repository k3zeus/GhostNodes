# Shared capabilities

This area will contain reusable tools selected by a base node manifest. Vault, Hermes, Tailscale, Nostr and NetBird are capabilities, not base nodes.

Each capability must declare configuration, data ownership, exposed ports, secrets, supported base nodes and validation before it is migrated here.
